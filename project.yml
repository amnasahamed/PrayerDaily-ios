name: offline-build
options:
  bundleIdPrefix: com.example
  developmentLanguage: en

settings:
  base:
    PRODUCT_BUNDLE_IDENTIFIER: com.example.offlinebuild
    PRODUCT_NAME: offline-build
    SWIFT_VERSION: 5.0
    MARKETING_VERSION: 1.0
    CURRENT_PROJECT_VERSION: 1
    SUPPORTED_PLATFORMS: iphoneos iphonesimulator
    TARGETED_DEVICE_FAMILY: '1'
    SUPPORTS_MACCATALYST: NO
    ALWAYS_SEARCH_USER_PATHS: NO
    ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon

targets:
  offline-build:
    type: application
    platform: iOS
    deploymentTarget: '17.0'
    preBuildScripts:
      - name: Generate App Icon
        script: |
          cd "$SRCROOT/offline-build"
          python3 generate_icon.py || true
    sources:
      - path: offline-build
    info:
      path: offline-build/Info.plist
      properties:
        CFBundleDevelopmentRegion: $(DEVELOPMENT_LANGUAGE)
        CFBundleDisplayName: PrayerDaily
        CFBundleExecutable: $(EXECUTABLE_NAME)
        CFBundleIdentifier: $(PRODUCT_BUNDLE_IDENTIFIER)
        CFBundleInfoDictionaryVersion: 6.0
        CFBundleName: $(PRODUCT_NAME)
        CFBundlePackageType: $(PRODUCT_BUNDLE_PACKAGE_TYPE)
        CFBundleShortVersionString: $(MARKETING_VERSION)
        CFBundleVersion: $(CURRENT_PROJECT_VERSION)
        CFBundleIconName: AppIcon
        ITSAppUsesNonExemptEncryption: false
        LSRequiresIPhoneOS: true
        NSLocationWhenInUseUsageDescription: "Aleha needs your location to calculate accurate prayer times for your area."
        NSMicrophoneUsageDescription: "Aleha does not use the microphone."
        NSCameraUsageDescription: "Aleha does not use the camera."
        NSUserNotificationsUsageDescription: "Aleha sends prayer time reminders and a daily Quran verse."
        UIBackgroundModes:
          - fetch
          - processing
        UIApplicationSupportsIndirectInputEvents: true
        UILaunchStoryboardName: LaunchScreen
        UIRequiredDeviceCapabilities: [arm64]
        UISupportedInterfaceOrientations:
          - UIInterfaceOrientationPortrait
          - UIInterfaceOrientationLandscapeLeft
          - UIInterfaceOrientationLandscapeRight
        UISupportedInterfaceOrientations~ipad:
          - UIInterfaceOrientationPortrait
          - UIInterfaceOrientationPortraitUpsideDown
          - UIInterfaceOrientationLandscapeLeft
          - UIInterfaceOrientationLandscapeRight

schemes:
  offline-build:
    build:
      targets:
        offline-build: all
    run:
      config: Debug
    archive:
      config: Release
